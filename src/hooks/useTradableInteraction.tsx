import { ModalInfo } from "../types"

interface TradableInterfaceProps {
    changeModal: (modalInfo:ModalInfo) => null;
}

const useTradableInteraction = ({}:TradableInterfaceProps) => {
  return (
    <div>useTradableInteraction</div>
  )
}
export default useTradableInteraction